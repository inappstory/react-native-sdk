import * as React from 'react';
import {StoryManager} from "../index";


declare type Props = {
  storyManager: StoryManager
};

export default function StoryReader({storyManager}: Props): React.ReactElement;
export {};